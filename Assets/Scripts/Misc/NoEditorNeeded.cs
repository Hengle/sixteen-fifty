using System;

namespace SixteenFifty {
  /**
   * \brief
   * Used by the subtype editor system to avoid looking for editors
   * for classes that have no parameters.
   */
  public class NoEditorNeeded : Attribute {
  }
}
